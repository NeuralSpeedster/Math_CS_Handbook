name: Compile Typst Documents
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Compile Typst Files
      uses: NeuralSpeedster/Math_CS_Handbook@main
      with:
        source_paths: 'main.typ'
        output_paths: 'main.pdf'